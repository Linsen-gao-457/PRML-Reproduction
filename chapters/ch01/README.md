# Chapter 1: Introduction

Run experiments from the repository root. Each experiment reads its defaults
from `configs/ch01/` and writes generated images to `outputs/figures/`.

```bash
python -m chapters.ch01 1
python -m chapters.ch01 4
python -m chapters.ch01 all
```
